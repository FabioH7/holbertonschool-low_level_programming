#include <unistd.h>
#include "main.h"

/**
 * mul - main function
 * @a: first parameter
 * @b: second parameter
 * Return: 1 if uppercase and 0 if false
 */

int mul(int a, int b)
{
	return (a * b);
}
